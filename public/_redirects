# Netlify _redirects file (backup for SPA routing)
# This ensures client-side routing works for known routes

# Known routes - serve directly (no redirect)
/            /index.html   200
/resume      /index.html   200

# Redirect old/unknown routes to 404 page
/*           /404.html     404
